CPE-1790: escape MacroParamPrompt's label render too (review round 3 laundering finding)

Review round 3 (independent adjudicator, PR 949): the round-2 comment left `label` unescaped in
MacroParamPrompt.svelte, giving a plausible-sounding reason -- "it doubles as the for=/id= value pairing
the <label> with its <input>, so escaping only the display text would desynchronize that pairing" -- that
does not hold. `for="macro-param-{label}"`, `id="macro-param-{label}"`, `data-testid="param-field-{label}"`,
and the `values[label]` object key all reference the raw `label` string directly; none of them are DOM
render positions this module's engine scans (only title=/aria-label=/alt=/body text are), so a bidi
override sitting in an un-rendered attribute value has no spoofing effect to begin with. Wrapping only
the `<label>` text node -- the file's one actual render position for this value -- touches the for/id
pairing in no way at all. A plausible technical justification for leaving a disclosed gap unfixed is the
same failure species as a false "static caller" claim: both stop the next person from checking.

Fixed: `{label}` -> `{displaySafeName(label)}` at the text node only (for=/id=/data-testid=/
values[label] left exactly as they were). MacroParamPrompt.svelte's REGISTRY entry drops to `[]` --
fully provably safe. Corrected the doc comments in both MacroParamPrompt.svelte and
bidiRenderScan.ts's CANDIDATE_PATTERN doc to state the real reasoning instead of the rejected one.

Also fixed an unrelated ratchet trip the new comment text introduced: "(PR #949)" reads as a 3-digit
hex color literal to app.css.test.ts's hard-coded-hex ratchet (`/#[0-9a-fA-F]{3,8}\b/g` matches "#949"
verbatim); reworded to "PR 949" (no leading `#`).

Verified load-bearing: reverted the wrap at the <label> text node, reran the guard --
"MacroParamPrompt.svelte: NEW raw offender(s) (line:expr): 79:label -- full found [79:label] vs
recorded []" -- then restored. Reconfirmed green: bidiEscape.guard.test.ts 13/13, app.css.test.ts 7/7
(hex ratchet passing again), full suite 315 files / 4163 tests, npm run check 0 errors/warnings.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01LK36xnZMb6koRUipkq1mtS
