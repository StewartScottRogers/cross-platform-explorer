[package]
name = "note-taker"
version = "0.1.0"
edition = "2021"
description = "UAT scratch probe (CPE-1699 real-CI red proof) — not a real feature, deleted immediately after."

[dependencies]
sidecar-contract = { path = "../contract" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
