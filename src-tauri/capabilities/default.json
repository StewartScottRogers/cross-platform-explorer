{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window, the torn-off preview window, and the standalone Agent Board window.",
  "windows": ["main", "preview-float", "agent-board"],
  "permissions": [
    "core:default",
    "core:app:allow-version",
    "core:event:default",
    "core:webview:allow-create-webview-window",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "opener:default",
    "opener:allow-open-path",
    "dialog:default",
    "process:default",
    "cli:default",
    "updater:default",
    "window-state:default"
  ]
}
