NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. NOT A REAL MP3 FILE - CPE-1702 deliberate corrupt-media fixture for the gui-smoke graceful-degrade regression case (samples/README.md). No MPEG frame sync bytes anywhere in this file on purpose, so GStreamer/WebKitGTK (and every other real MP3 decoder) must fail to decode it regardless of which plugin packages are installed. 